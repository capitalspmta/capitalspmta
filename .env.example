# Banco (LOCAL por padrão): SQLite (não precisa instalar Postgres para testar)
# Produção: use PostgreSQL do seu provedor/host.
# Exemplos:
# - Local (SQLite): DATABASE_URL="file:./dev.db"
# - Docker (Postgres): DATABASE_URL="postgresql://panel:panel@db:5432/panel?schema=public"
# - Windows (Postgres): DATABASE_URL="postgresql://usuario:senha@localhost:5432/panel?schema=public"
DATABASE_URL="file:./dev.db"

# Base pública do site (para links em e-mail e redirects). No NGROK FREE, atualize quando o link mudar.
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# JWT (sessão)
JWT_SECRET="askjdha2!@*&¨#NKJD7fyqa@DSudhsdadFA**sdhas(d87"

# Mercado Pago (opcional)
MP_ACCESS_TOKEN=""
MP_WEBHOOK_SECRET=""  # se você usar validação HMAC/assinatura
MP_NOTIFICATION_URL="http://localhost:3000/api/payments/mercadopago/webhook"

# SMTP (obrigatório para verificação de e-mail e reset de senha)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="no-reply@seudominio.com"

# API externa (compras/token) e integração MTA
STORE_API_TOKEN="troque-para-um-token-grande"

# Chave que o resource do MTA envia no header X-MTA-KEY.
# Use a MESMA string em: mta/site/config.lua -> API_KEY
MTA_API_KEY="troque-para-um-token-grande"

# (Compat) versões antigas usavam este nome. Pode deixar vazio.
MTA_API_TOKEN=""

# Discord OAuth (opcional)
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
# Se vazio, usa http://localhost:3000/api/discord/callback
DISCORD_REDIRECT_URI=""
